# Local media copies

These files are local copies of media that was previously hosted by the old site platform.

Use root-relative paths such as:

```text
/images/teamlinkt/2025_coach_cheshire_crop5.png
```

That path works on Hugo, GitHub Pages, and Cloudflare Pages because everything under `static/` is copied to the published site root.
